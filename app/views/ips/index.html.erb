<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first active"><%= link_to "#{t("web-app-theme.list", :default => "List")}", ips_path  %></li>
      <li><%= link_to "#{t("web-app-theme.new", :default => "New")}", new_ip_path %></li>
    </ul>
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> Ips</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
                    <th>
            <%= t("activerecord.attributes.ip.number", :default => t("activerecord.labels.number", :default => "Number")) %>
          </th>
                    <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @ips.each do |ip| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= ip.id %>
          </td>
                    <td>
            <%= link_to ip.number, ip_path(ip) %>
          </td>
                    <td>
            <%= ip.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", ip_path(ip) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_ip_path(ip) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", ip_path(ip), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
