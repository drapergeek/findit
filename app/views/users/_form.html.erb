<%= simple_form_for @user, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :login %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.input :last_login %>
  <%= f.input :last_login_ip %>
  <%= f.input :can_login %>
  <%= f.input :email %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                users_path, :class => 'btn' %>
  </div>
<% end %>
